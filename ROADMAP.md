# ROADMAP

## v2.0 — 進行中

部品数ではなく、**利用者がしなくてよくなった判断の種類**を増やします。
外部 worklist の候補は、そのまま実装せず [`docs/plan-v2.md`](docs/plan-v2.md) の
複雑さ予算と責任境界で分類しました。

- [x] 候補を「既存 item へ吸収 / 独立責任 / 条件付き / public primitive にしない」へ分類
- [x] 同期の二重操作防止に完全な Action state machine を要求しない層の原則を追加
- [x] 軽量 interaction guard（`useInteractionGuard`: `tryLock` / `release` だけ）
- [x] `layout` へ Switcher / SidebarLayout の判断を吸収
- [x] library非依存のvalidation contract / AsyncForm / server Response adapter
- [ ] FieldArray（stable key / min-max / focus / nested path）
- [ ] ErrorBoundary / useAutosave
- [ ] Popover foundation / Paginator / CopyButton
- [ ] 条件を満たした behavioral recipe
- [ ] checker の intentional-break 検証と CI の重複・実行時間改善
- [ ] v1 → v2 migration、全 public 面、実 install、Pages smoke、immutable Release

Wizard / Editable / InfiniteList は、巨大な API を先に固定せず recipe または
Load more contract で需要と共通部分を観測します。採用しない判断も v2 の成果に含めます。

## v0.1 — 完了

契約層とトンマナ層が固まり、状態を持つコンポーネントが動いています。

- [x] `Action` / `ActionSpec` / `ActionError` の契約
- [x] `useAction`（二重送信防止・中断・自動リトライ・自動リセット）
- [x] `useResource`（依存キーでの再取得・アンマウント中断）
- [x] トンマナ 4 種 × light/dark
- [x] `ActionButton` / `AsyncForm` + `Field` / `DataList` / `AsyncBoundary`
- [x] `ThemeProvider` / `ThemeSwitcher` / ちらつき防止スクリプト
- [x] React + Vite のカタログ / Astro 静的サイト + island 連携
- [x] shadcn レジストリ生成 + 利用者プロジェクトでの型検査

## v0.2 — 完了

「配置で苦しむ」への答えと、エラー処理の安全網。

- [x] **レイアウト・プリミティブ**
      `Box` / `Stack` / `Inline` / `Columns` + `Column` / `Tiles` / `Spread` /
      `PageBlock` / `ContentBlock` / `Section` / `Divider`
- [x] 余白スケール 9 段階。テーマごとに実寸が変わる
- [x] **段階外の値も書ける**（`space="13px"` / `"clamp(...)"` / `width="18rem"` /
      `columns="repeat(auto-fill, minmax(14rem, 1fr))"`）。
      段階は既定値であって制約ではない
- [x] 画面幅ごとの指定（`space={{ mobile: "sm", tablet: "3rem" }}`。段階と任意の混在も可）
- [x] `Columns` は既定でタブレット幅未満に縦へ畳む
- [x] `ActionDefaults` / `ActionProvider` / `Toast` / `useToast`
- [x] 二重表示の抑制ルール（画面内に出せているものは通知しない）
- [x] 実ブラウザ検証（`scripts/verify-v02.mjs`）

### v0.2 で踏んだ罠（同じことを繰り返さないための記録）

**1. 余白トークンを Tailwind 標準の `--spacing-*` 名前空間に入れてはいけない。**
`--spacing-sm: 0.75rem` を定義すると `max-w-sm` が 24rem ではなく 0.75rem になります。
`sm` / `md` / `lg` は t シャツサイズと衝突するためです。
`@utility gap-* { gap: --value(--space-*); }` の形で独自ユーティリティとして
定義すれば、標準クラスを一切壊しません。

**2. `@theme` は `static` を付けないと変数が消える。**
既定の `@theme` は「CSS 内で使われている変数」しか出力しません。
段階の値をインライン style から `var(--space-3xl)` として参照する設計にすると、
Tailwind からは未使用に見えて変数ごと削除され、余白が 0 になります。
`@theme static { ... }` が必須です。

**3. 制約は壁ではなく既定値にする。**
最初は `space` の型を 9 段階のリテラルに固定しましたが、
「自由度の確保」という目的と、自分で書いた設計原則②（エスケープハッチを必ず残す）に
反していました。`SpaceToken | (string & {})` にすることで、
補完には段階だけを出しつつ任意の値も受けられます。

## v0.3 — 完了

端末幅で崩れないこと、崩れていないと確かめられること。

- [x] 壊れない土台（折り返し / メディアの最大幅 / `pre` のスクロール / `min-width:0`）
- [x] 触れる端末でのタップ領域の最低保証（`@media (pointer: coarse)`）
- [x] 入力欄を常に 16px 以上に（iOS の自動拡大を止める）
- [x] `Scrollable` — 縮められない中身をその部分だけ横スクロールに
- [x] `ContentBlock width="prose"` — em 単位で文字サイズに追従する本文幅
- [x] `npm run check` — 5 つの幅で機械的に検出。崩れがあれば終了コード 1
- [x] カタログに端末プレビュー（実ブラウザ幅で iframe 表示）
- [x] 監査スクリプト 2 本（`audit-stress` / `check-responsive`）

### 実測（v0.3 適用前 → 後）

```
折り返せない長い URL       320px で 577px はみ出し  →  ✓
長い英単語の羅列           320px で 349px はみ出し  →  ✓
入力欄の font-size         全 7 箇所が 16px 未満    →  ✓
タップ領域                 2 件が 24px 未満         →  ✓
1 行の長さ                 6 件が長すぎ             →  ✓
```

`npm run check` が 2 ページ × 5 幅すべてで緑になりました。

### v0.3 で踏んだ罠

**1. スクロール領域の中身を「はみ出し」と誤検出した。**
`pre` や `Scrollable` の中身は親の外へ出ていて当然です。
祖先に `overflow-x: auto|scroll|hidden` があるかを見て除外する必要があります。

**2. 1 行の長さの閾値を欧文基準で作ってしまった。**
和文は 1 文字がほぼ 1em、欧文は平均 0.5em 前後。同じ 704px でも
欧文なら 100 字、和文なら 50 字で、読みやすさが倍違います。
和文比率を見て閾値を変えないと、日本語サイトが常に誤検出されます。

**3. `md:text-sm` では iOS の自動拡大を防げない。**
拡大は画面幅ではなくフォントサイズで起きるので、iPad でも発生します。
入力欄は常に 16px 以上にするしかありません。

## v0.3.5 — 完了（リファクタリング）

部品を増やす前に土台を整えました。

- [x] `theme.css`（557 行）を `tokens.css`（土台 + 既定テーマ）と
      `themes.css`（追加 3 テーマ）に分割。**自分のテーマを作れるようになった**
- [x] レジストリも `@nasu/tokens` と `@nasu/theme` に分離。
      `@nasu/layout` は tokens にだけ依存（テーマ無しでも動く）
- [x] デモを配布物のレイアウト部品で組み直し。生の `flex flex-col gap-*` を一掃
- [x] `Panel` の重複を解消し、`App.tsx` のローカル `Section` の名前衝突を解消
- [x] ちらつき防止スクリプトの 3 重重複を解消（定義は `themeInitScript` の 1 箇所だけ）
- [x] スクリプト 10 本 → 8 本。死んでいた `audit-responsive.mjs` を削除、
      `verify-states2` を統合、`verify-v02` → `verify-layout` に改名
- [x] `pnpm verify` で 10 項目を 1 コマンド実行
- [x] GitHub Actions（push / PR / 週 1）+ Renovate
- [x] `Columns_` → `TileColumns`

### リファクタ中に見つけて直したもの

**1. `Stack dividers` の線が左右非対称だった。**
`divide-y` は要素の下端に線を付けるので、`gap` と併用すると
線の上が 0、下が gap 分になります。区切り線を**実際の要素として差し込む**形に変更し、
上下とも同じ余白（実測 24px / 24px）になりました。

**2. `Inline wrap={false}` が子を潰していた。**
`.wt-gap > * { min-width: 0 }` が効いて、幅 400px に 3 つ入れると
261/163/49px まで圧縮されていました。折り返さない指定なら
**潰さず横スクロール**が正しいので（`Scrollable` と同じ考え方）、
`min-width` を解除して `overflow-x: auto` にしました。

## v0.4 — 完了

状態を持つ部品を 4 つ。**見た目だけの部品は引き続き作っていません。**

- [x] `ConfirmDialog` / `useConfirm` — native `<dialog>`。Provider が無ければ
      `window.confirm` に落ちるので、置かなくても壊れない
- [x] `DataTable` — 並べ替え・ページング。**狭い画面では 1 行 = 1 カードに組み替え**
- [x] `AsyncSelect` — debounce + 前の要求の自動中断 + WAI-ARIA combobox
- [x] `FileDrop` + `uploadWithProgress` — 進捗つき、1 ファイルずつ、失敗分だけ再送
- [x] カタログに「部品」タブ
- [x] `scripts/verify-parts.mjs`（14 項目）を `pnpm verify` に追加 → 全 11 項目

### 計画に書いた「実測で確かめる項目」の結果

計画段階で 10 個列挙しておいたものを全部測りました。

```
1. dialog::backdrop     rgba(0,0,0,0.45) が効く。:modal にも一致（top layer）
2. Provider 無しの confirm  window.confirm に落ちる
3. Scrollable 内の表     min-width 576px で潰れない
4. 320px 最下部の候補    上向きに出て画面内に収まる（自前実装で足りた）
5. キーボード操作        ↑↓ で activedescendant が動き、Enter で確定
6. XHR の中断            signal で abort される
7. 失敗分だけ再送        2 件中 1 件失敗 → 再送ボタンは 1 つ
8. pnpm check            2 ページ × 5 幅すべて緑
9. カードの列名          日付/案件/状態/件数/金額 が全部読める
10. 打ち直しの競合        古い候補が残らない
```

**AsyncSelect の位置決めは自前で足りました。** 320px・画面最下部で
候補が上向きに出て、画面内（上端 292 / 下端 552 / 画面高 600）に収まりました。
floating-ui への差し替えは不要と判断します。

### v0.4 で見つけて直したもの

**エラーが二重に出ていた。** FileDrop が失敗を行内に出しつつ
`ActionProvider` の通知にも流していました。
v0.2 で決めた「画面内に出せているものは通知しない」に反していたので、
`onError` を任意の口として切り出し、既定では通知しないようにしました。

**`id` が固定で衝突していた。** FileDrop の `<input type="file">` が
`id="wt-file-input"` 固定だったので、1 画面に 2 つ置くと `label` の
関連付けが壊れます。`useId` に変更。

## v0.5 — 完了

- [x] **`AsyncForm` の FormData バグ修正**（入力部品の前提）
- [x] 入力部品 `SelectField` / `CheckboxField` / `CheckboxGroup` /
      `RadioGroup` / `DateField`
- [x] `DataTable` の行選択（キーで保持・Shift+クリック・カード表示にも対応）
- [x] `useOptimisticList` — 楽観更新と、失敗した操作だけのロールバック
- [x] カタログに「入力/選択」タブ
- [x] `scripts/verify-forms.mjs`（15 項目）を追加 → `pnpm verify` は 12 項目に

### 計画に書いた「実測で確かめる項目」の結果

```
 1. indeterminate     プロパティとして true、属性としては存在しない ✓
 2. ページ移動        1 件選択中 → 表示中を全選択で 6 件 ✓
 3. 伝播しない        チェックで onRowClick が発火しない ✓
 4. 並べ替え          選択件数 4 件を維持 ✓
 5. Shift+クリック    0→3 の範囲で 4 件 ✓
 6. 追加失敗          削除した項目が復活しない ✓
 7. 直列化            同一キーの update を順に流す ✓
 8. 再取得            保留中: あり → あり（消えない）✓
 9. 複数選択          langs が配列 3 件 ✓
10. 未チェック        agree が "" として届く ✓
11. タップ領域        ラベルの高さ 44px ✓
12. 日付の文字        16px ✓
13. RadioGroup       fieldset + legend ✓
14. pnpm check       2 ページ × 5 幅すべて緑 ✓
```

### v0.5 で見つけて直したもの

**1. `AsyncForm` が複数値を取りこぼしていた（計画段階で発見）。**
`Object.fromEntries(fd.entries())` は同じキーが複数あると最後だけ残すので、
`<select multiple>` で 3 つ選んでも 1 つしか送られませんでした。
同名を配列に畳む `formDataToObject` に置換。
未チェックのチェックボックスは FormData に現れないため、
隠し入力で目印を送り、`""` として必ず届くようにしました。

**2. 整形した列を、整形後の文字列で並べ替えていた（検証中に発見）。**
`get: (r) => \`¥${r.amount.toLocaleString()}\`` のような列を並べ替えると、
`¥2,600` が `¥12,400` より後ろに来ていました（"2" > "1" のため）。
並べ替えは **`get` の結果ではなく元の値**を使うように変更し、
別のキーで並べたいとき用に `sortValue` を足しました。
数値らしい文字列も数値として比較します。

**3. ソースに NUL バイトが混入していた。**
チェックボックスの目印文字列に制御文字が入り、ファイルがバイナリ扱いに
なっていました。`__wt_unchecked__` に変更。

## v0.5.5 — 完了（リファクタリング）

詳細は [docs/refactor-v05.md](docs/refactor-v05.md)。

**この回の要点は「検査の穴」でした。** `pnpm check` はカタログの URL を 1 つしか
渡しておらず、カタログはタブで中身が入れ替わる画面だったので、
**v0.4 と v0.5 の部品は 320px で一度も検査されていませんでした。**

### 直したもの

| | 内容 |
|---|---|
| 検査の穴 | 端末幅の検査対象を 1 ページ → 6 ページ（全タブ）へ。`?tab=` を実装 |
| 検査の誤り | sr-only の入力／`<label>` の当たり判定／スクロール領域内の `min-width`／URL を「長い行」と呼ぶ、の 4 種の誤検知 |
| 本物の指摘 | 表の中のチェックボックス 20×20、並べ替えボタン 44×20、デモの裸のチェックボックス |
| 配布物 | `registryDependencies` の漏れ **9 項目**。`check-registry-deps.mjs` を新設して常設 |
| 中断 | `useOptimisticList` の `AbortController` が誰にも中断されていなかった |
| 重複 | 入力欄の class が 4 か所にコピー → `inputClass()` へ集約 |
| id | `ConfirmProvider` の固定 id を `useId` へ |

### 覚えておくこと

- **画面がタブや分岐で切り替わるなら、検査対象も切り替えないと意味がない。**
  「検査は通っている」と「検査対象に入っている」は別のことです。
- **`registryDependencies` の漏れは、このリポジトリでは絶対に再現しません。**
  全ファイルが揃っているからです。壊れるのは利用者のところだけ。
  だから import 文から機械的に突き合わせる検査が要ります。
- **表の中で `w-full` は効かない。** 列幅は中身から決まるので循環します。`min-width` を使います。
- **`new AbortController().signal` を渡すのは、中断できるふりでしかない。**
  controller を誰かが持っていて、いつか `abort()` を呼ばないと意味がありません。

## v0.6 — 完了（ナビゲーション・開閉部品・本文・画像）

計画は [docs/plan-v06.md](docs/plan-v06.md)、結果は [docs/result-v06.md](docs/result-v06.md)。

配布アイテムは 25 → 33。`pnpm verify` は 14 工程、端末幅の検査は 8 ページ × 5 幅。

### 作ったもの

| | 中身 |
|---|---|
| ナビ | `SiteHeader` / `NavLink` / `SkipLink`。狭い画面のメニューは `<details>` |
| 開閉 | `Dialog`（中央 / シート）・`Tabs`・`Disclosure` / `Accordion`・`DropdownMenu` / `NavDropdown` |
| 本文 | `prose.css`（幅は持たない） |
| 画像 | `Frame`（比率を先に確保）/ `Img`（`priority`） |
| 共通 | `usePopover`（外側クリック・Esc・出す向き） |

### 設計で変えた判断

**狭い画面のメニューを `<dialog>` ではなく `<details>` にしました。**
計画では `Dialog` を使い回すつもりでしたが、それだと
**JavaScript が届くまでメニューが開けません**。Astro の島にした場合、
回線が遅いと「押しても何も起きない数秒」が生まれます。
`<details>` ならブラウザの機能なので JS 0 で開閉し、`client:` すら要りません。
**実測でも、JavaScript を切った状態でメニューが開きました。**

### 見つけて直したもの

**1. アンカーリンクが貼り付いたヘッダに潜り込んでいた。**
`#contact` へ飛ぶと、見出しが 64px ぶん完全に隠れていました（実測）。
`--header-h` を 1 つ置き、ヘッダの高さと `html` の `scroll-padding-top` が
**同じ値を共有する**ようにしました。ヘッダを高くしてもアンカーはついてきます。

**2. `DropdownMenu` を Esc で閉じるとフォーカスが body へ落ちていた。**
キーボードだけの人は、閉じるたびにページ先頭から辿り直しになります。
`usePopover` が「なぜ閉じたか」（`escape` / `outside`）を渡すようにし、
Esc のときだけ開いたボタンへ戻します。外側を押したときは戻しません
（押した先からフォーカスを奪うため）。

**3. `<dialog>` を開いても背面のスクロールは止まらない。**
ブラウザが止めるのは「押せること」だけです。開いたまま指で払うと裏が動きます。
開いている数を数えて、最後の 1 つが閉じたときだけ戻すようにしました。
開閉で画面が横に揺れないよう `scrollbar-gutter: stable` も入れています。

**4. タブを正しい `role="tab"` にしたら、既存の検査が壊れた。**
`verify-states.mjs` が `getByRole("button", { name: "状態" })` でタブを
押していたためです。**壊れたのは正しさの証拠**なので、
検査側を URL 指定（`?tab=`）へ寄せました。

### 検査の誤りも 6 つ直しました

作ったものより、**測り方の間違いのほうが多かった**回です。

| 誤り | 何が間違っていたか |
|---|---|
| 背面スクロール | `window.scrollBy` で測っていた。あれはプログラム操作なので `overflow: hidden` でも動く。ホイール入力で測り直し |
| フォーカストラップ | `activeElement` が body になるのを「外へ出た」と数えていた。Chrome はモーダルの端でブラウザ UI へ移る。背面は inert なので、ページ内要素を掴まないことを見るのが正しい |
| タブの横スクロール | 広い画面で測っていたので入りきっていた |
| summary の高さ | ヘッダのハンバーガーを拾っていた（広い画面では高さ 0） |
| 見出しの余白 | 先頭の要素で測っていた（そこは 0 が正しい） |
| 本文中のリンク | 19px を「タップ領域が小さい」と叱っていた。**WCAG 2.1 の 2.5.8 には「インライン」の例外**があり、文中のリンクは対象外 |

### 覚えておくこと

- **JS 無しで動くなら、そちらを選ぶ。** `<details>` は開閉・キーボード・
  読み上げ・ページ内検索を全部持っています。`useState` で書くと全部失います。
- **`role` を正しくすると、`getByRole("button")` の検査は壊れる。**
  壊れたら直す前に、それが正しさの証拠でないか確かめること。
- **同じ値を 2 か所に書かない。** ヘッダの高さとアンカーの逃げ幅は同じものです。
- **`window.scrollBy` はユーザー操作の代わりにならない。**

## v0.6.5 — 完了（検査を「見る」から「落ちる」へ）

計画は [docs/plan-v065.md](docs/plan-v065.md)。

### 何が問題だったか

**86 個の測定に、判定が 1 つもありませんでした。**

```
verify-states  測定 10 → 判定 0     verify-forms  測定 17 → 判定 0
verify-layout  測定  9 → 判定 0     verify-nav    測定 33 → 判定 0
verify-parts   測定 14 → 判定 0     audit-stress  測定  3 → 判定 0
```

全部 `log()` で数字を出しているだけで、機械的に落ちるのは例外が飛んだときだけ。
タップ領域が 44px から 20px に戻っても、`aria-current` が消えても、
**数字が印字されて緑のまま**通っていました。
これまで見つけたバグは全部、その印字を人が読んで気づいたものです。

### やったこと

- `_browser.mjs` に `must` / `mustEq` を追加。**106 件の判定**に変換
- `audit-stress.mjs` に終了コードを追加（崩れたら落ちる）
- **製品のコードには一切触っていません。** 触ったのは `scripts/` だけです

### 何を判定にして、何をしないか

| | 扱い |
|---|---|
| 壊れたら困る性質（タップ領域 24px 以上・role・フォーカスの戻り先・送信値の形） | 判定にする |
| トークンから決まる値（余白が段階のどれか・prose の幅） | 判定にする |
| 配置で変わる値（要素の絶対座標） | **判定にしない。**「動いた／動かない」という関係を判定にする |

厳しすぎる判定は、無関係な変更で落ちて、やがて誰も見なくなります。

### わざと壊して、本当に落ちるか確かめました

| 壊した内容 | 結果 |
|---|---|
| `Tabs` の tabIndex を全部 0 にする | ✓ roving tabindex の判定だけが落ちた |
| `formDataToObject` を `Object.fromEntries` に戻す | ✓ 複数値の判定 2 件だけが落ちた |
| `NavLink` の `min-h-11` を消す | **✗ 落ちなかった** |

3 つ目で**検査側の穴**が見つかりました。WCAG 2.5.8 のインライン例外を
`display.startsWith("inline")` で書いていたため、ナビゲーションのリンク
（`<li>` の中の `inline-flex`）まで例外に入っていました。

地の文のリンクは `display: inline` です。`inline-flex` はレイアウトのために
明示的に選んだもので、文章の一部ではありません。
`cs.display === "inline"` に絞ったところ、狙いどおり検出されるようになりました。

**「判定を足しただけでは、落ちるかどうかは分からない」**——
わざと壊す手順を踏まなければ、この穴は残ったままでした。

### Windows

配布物に OS 依存はありません。開発用スクリプトだけの話です。
**v0.9a で実機確認しました**（Windows 11 / Node 24.13 / pnpm 10.28）。
コードを読んで書いた対応のうち、**2 つは実際には間違っていました。**
詳しくは v0.9a の節を見てください。

## 積み残し（優先度順）

- [ ] `Field` にリアルタイム検証（送信前に気づける）
- [ ] `DataTable` の列の表示/非表示切り替え
- [ ] 一括操作の進捗表示（`selectionActions` から `useAction` へ繋ぐ）

## v0.7 前半 — 完了（SEO 基盤とブログ）

計画は [docs/plan-v07.md](docs/plan-v07.md)、結果は [docs/result-v07a.md](docs/result-v07a.md)。

判定 106 → **172 件**、検査 14 → 16 工程、端末幅の検査対象 8 → 11 ページ。

### 作ったもの

- `lib/seo.ts` / `lib/feed.ts`（**純粋な関数**。`@astrojs/rss` も `@astrojs/sitemap` も使わない）
- ブログ雛型（Content Layer / 一覧 / 記事 / prose）
- `sitemap.xml` / `rss.xml` / `robots.txt` / `404`

### 見つけて直したもの

**1. JSON-LD の中の `<` を逃がしていなかった。**
記事名に `</script>` が入ると、そこでスクリプトが終わって残りが本文として出ます。
差し込みの穴にもなります。`JSON.stringify` は `<` を逃がしません。

**2. トップページに `description` が無かった。**
判定を入れていなければ、印字を読み飛ばして終わっていました。

### 既存の部品に手を入れた 1 件

`SiteHeader` に `brandHref`（26 行）。**`.astro` では props に要素を書けません。**

```astro
<SiteHeader brand={<a href="/">…</a>} />   ← ビルドが落ちる
```

`brand` を `React.ReactNode` にしていたのは React 側だけを見た設計でした。
Astro の island に渡せるのは JSON になる値だけ——**v0.2 で `ActionSpec` を足したときと同じ穴**です。

### 覚えておくこと

- **下書きの除外は 1 か所に集約する。** 一覧・sitemap・RSS の 3 か所で個別に絞ると、
  どれかで漏れる。しかも自分の文章なので見ても気づけない
- **検査対象は sitemap から取る。** 手で並べると、ページを足したとき漏れる
- **黙って縮退しない。** sitemap が取れないときは理由を印字する。
  でないと「全部見たつもり」になる

## v0.7 後半 — 完了（雛型と画像）

結果は [docs/result-v07b.md](docs/result-v07b.md)。判定 172 → **214 件**、
Astro サイトの検査対象 4 → 7 ページ。

### 作ったもの

- ランディング（`/lp/`）/ 会社概要（`/about/`）/ お問い合わせ（`/contact/`）
- `SiteFooter`（リンク群に見出しを付け、当たり判定 44px）
- ナビとフッタの定義を `src/lib/nav.ts` 1 か所へ

**見た目のセクション集は作っていません。** 用意したのは骨格と配線です
（見出しの階層 / landmark / 画像の場所取り / JS 無しで開く FAQ）。

### Markdown の画像

実測で分かったこと。

| 書き方 | 結果 |
|---|---|
| `![図](./diagram.png)` 相対パス | Astro が `width` / `height` を付ける → ずれない |
| `![図](/img/diagram.png)` 絶対パス | 素の `<img>` のまま → **読み込みで本文がずれる** |

sharp を入れなくても寸法は付きます（`passthroughImageService`）。
最適化は諦めますが、**ずれないことのほうが大事**で、
native の依存を増やさずに済みます。

### 検査の作り直し（3 件）

**1. 画像の場所取りを、推測から実測へ。**
属性から推測する方式は、寸法の無い画像を置いても**1 件も検出できませんでした。**
プレビューが速く、測るときには既に読み終わっていたためです。

**画像のリクエストを遮断して開く**方式に変えました。遅い回線と同じ状態です。
高さの数値でも区別できません（届かなかった画像は代替テキストの高さになり、
実測 30px で `h-8` と同じ）。代替テキストを一時的に消して高さが消えるかで判定します
（場所を取っていない画像 16px / 取っている画像 185px）。

**2. `flex-shrink: 0` の判定が縦並びでも効いていた。**
flex-shrink は主軸の話なので、親が縦並びなら幅とは無関係です。
狭い画面で畳んだ段組みが毎回引っかかっていました（実測 343px / 375px）。
親が横並びのときだけ見るようにしました。

**3. 雛型ページの骨格を判定に追加。**
h1 が 1 つか / landmark が揃っているか / 見出しが飛び級していないか /
スキップリンクの飛び先が focus できるか。

### Astro と React の分け方

[docs/astro-and-react.md](docs/astro-and-react.md) に方針を書きました。
**2 つは同じプロジェクトに共存します**（`apps/site` がまさにそれ）。
だからフレームワークで部品を割らず、
「純粋な関数 / React 1 セット / 薄い包み」で分けています。


## v0.8 前半 — 完了（フォームの送信先）

計画は [docs/plan-v08.md](docs/plan-v08.md)、結果は [docs/result-v08a.md](docs/result-v08a.md)。

判定 214 → **242 件**、検査 16 → 17 工程。

1 通目に書いた「**予め関数を用意しておいて「これを呼び出してね」と決めておく**」を、
ここでやっと果たしました。

### 作ったもの

- `lib/submit.ts` の `createSubmit()`（タイムアウト / 失敗の翻訳 / 検証エラーの吸収 / おとり）
- `HoneypotField`（人にも読み上げにもキーボードにも到達させない）
- `examples/receivers/cloudflare-worker.ts`（CORS のプリフライト込み）
- `scripts/_endpoint.mjs`（**本物の HTTP を受けて測る**ための受け口）

### 見つけて直したもの（既存変更 1 件・報告）

**`jsonRequest` が成功時の `res.json()` を守っていませんでした。**

- 200 だが本文が空 → 落ちる（204 だけ特別扱いしていた）
- 200 だが HTML（プロキシや設定ミス）→ **`SyntaxError: Unexpected token '<'`**
  という技術的な文言がそのまま画面に出る

`content-type` を見て分岐し、空も HTML も落とさないようにしました。
`ActionSpec` を使う全員に効くので、`submit.ts` の中ではなく本体を直しています。

### 決めたこと

- **CORS の失敗は通信断と区別できません。**（ブラウザは理由を教えません）
  だから「送信先に接続できませんでした。通信状況か、送信先の設定（CORS の許可）を
  確認してください」と、**両方を含む言い方**にします。嘘の断定をしない
- **おとりに引っかかったら、成功したように見せます。** 弾いたと分かると学習されます
- **タイムアウトと「画面から消えた」は同じ AbortError ですが、文言を分けます。**
  前者は「時間内に応答がありませんでした」、後者は利用者に何も出しません

## v0.8 後半 — 完了（入口）

結果は [docs/result-v08b.md](docs/result-v08b.md)。全体像は [docs/overview.md](docs/overview.md)。

```
npx create-webtemplate my-site
```

判定 242 → **271 件**（うち入口 29 件）、検査 17 → 18 工程。

### 部品が 2 か所にならないようにした

テンプレートに部品をコピーして commit すると、原本と 2 か所になります。
**このリポジトリが何度も壊れた原因そのもの**なので、
テンプレートは `scripts/build-create-template.mjs` の生成物にして
`.gitignore` に入れました。手で編集する余地が無ければ、ずれようがありません。

### 生成物が本当に動くか、機械で確かめている

**「生成できた」では足りません。**

```
✓ 8. astro: pnpm install が通る      ✓ 8. vite: pnpm install が通る
✓ 9. astro: 型検査が通る             ✓ 9. vite: 型検査が通る
✓ 10. astro: ビルドが通る            ✓ 10. vite: ビルドが通る
✓ 11. astro: 配信して画面が出る       ✓ 11. vite: 配信して画面が出る
✓ 12. astro: 端末幅の検査を通る       ✓ 12. vite: 端末幅の検査を通る
```

### 見つけて直したもの（既存変更 3 件・報告）

**1. 足場のコードが型検査に通らなかった。**
`Button` に `action` を渡していました（それは `ActionButton` です）。
`vite build` は通るのに `tsc --noEmit` で落ちます。
**雛型が壊れたコードを配るところでした。**

**2. `SiteHeader` が、幅のある `actions` を渡されるとはみ出す。**
生成した Vite 版に `ThemeSwitcher` を置いたら、375px で 15px はみ出しました。
flex の子は既定で `min-width: auto` なので縮めません。`min-w-0` を追加。

**3. `ThemeSwitcher` が 320px で折り返せない。**
中のボタン群が `flex` のまま `min-w-0` を持っておらず、突き抜けていました。

**2 と 3 は同じ原因です。** flex の子は既定で縮まない——
`Inline`・表・ヘッダで、**3 回目**の遭遇でした。

### レジストリの不整合も 1 件

`prose.css` だけ `target` が `lib/` を指していました（他の CSS は `styles/`）。
`styles/prose.css` に揃えました。

## v0.9a — 配布（進行中）

計画は [docs/plan-v09.md](docs/plan-v09.md)、結果は [docs/result-v09a.md](docs/result-v09a.md)。

配布を開ける前に、**配るものが差し替えられない状態**を作る版です。
`public/r/*.json` を公開した瞬間、このリポジトリは
「他人のマシンでコードを実行する側」になります。順番を逆にすると、
開けてから塞ぐことになります。

- [x] **Windows の実機で緑にする**（`pnpm verify` 20 / `verify:create` 29）
- [x] 配布経路の防御（権限 / SHA 固定 / 依存の待機 / postinstall の許可制）
- [x] レジストリと入口を GitHub Pages に出す仕組み（`pages.yml`）
- [x] `npx shadcn add` を**本物の CLI で**通す（`verify-install-real.mjs`）
- [x] 作者が利用者として生成物を触り、**そこで見つかった 3 件を直す**
- [ ] **公開する**（Pages の有効化と ruleset は [docs/security.md](docs/security.md) §3）
- [ ] 404 のステータスを実測する（公開後に `verify-published.mjs` が測ります）

### 生成物を触って見つかったもの

**部品側の検査は全部緑でした。** 生成物を実際に使うまで表に出ません。

1. **生成したプロジェクトで部品を足せなかった。** README は「足す」と書いて
   いるのに `components.json` が無く、shadcn CLI が対話で止まる。作らせても
   `registries` が無いので `Unknown registry "@nasu"`。**案内どおりに進んだ人が
   2 回続けて詰まる**状態でした。テンプレートに同梱して解決
2. **生成 README が通らないコードを教えていた。** `<Button action={…}>` ——
   `Button` は実在しますが `action` を受け取りません（`ActionButton` です）。
   v0.8b で足場のコードは直したのに、**README の例が取り残されていました**
3. **上書き確認は `--yes` で覆えない。** 非対話だと終了コード 0 のまま
   何も書かずに終わります。利用者は N のままで構わないので、そう書きました

判定を 3 つ足しました。うち `8.5 本物の CLI で部品を足せる` が本命です
（設定の形を見るだけでは、通ることは確かめられません）。

### 利用者からの報告 7 件（全部実在した）

作者が実際に触って挙げてくれたものです。詳細は
[docs/result-v09a.md](docs/result-v09a.md) の Phase 5。

| | 内容 |
|---|---|
| 1 | `baseUrl` の非推奨警告。**生成物を開いた瞬間にエディタが警告を出す** |
| 2 | 広い画面で内容が左半分に寄る。器を広くして本文にだけ `max-w-prose` を付けていた |
| 3 | 生成直後の案内が流れて消える → `HowToUse.md` に書き出す |
| 4 | `npm run check` のパスが実体とずれていた + playwright 未導入時が生のスタック |
| 5 | 未公開なので部品を足せない（仕様どおり。欠陥ではない） |
| 6 | 文章が初心者向けでなかった（island / 静的 HTML などの用語） |
| 7 | 成功したボタンが hover で背景に溶ける |

### 依存を上げて分かったこと（報告 5 件）

**配っている astro が 2 メジャー遅れで、XSS / SSRF の勧告を 8 件抱えていました。**
`security.md` §6 に書いた「固定は勝手に直らない」が、そのまま現実になっています。
astro 7 / vite 8 へ更新し、生成物の脆弱性は 0 件になりました（判定 8.2 を追加）。

更新の副作用で、**検査の作りの誤りが 2 つ**表に出ました。

- `::backdrop` の判定が色の**表記**（`/rgba?\(/`）を見ていた。出力が oklch()
  のまま届いた瞬間、**部品は無事なのに検査だけが落ちました**
- `astro preview` が**デーモン**になった（別ポートへ逃げ、親 PID が先に死ぬ）。
  道具の preview を追うのをやめ、ビルド済みの中身を自分で配る形にしました
  （`scripts/_static.mjs` / `serve-static.mjs`）。**動く部品が 1 つ減りました**

**2 と 7 は、既存の検査では原理的に見えませんでした。**

- 端末幅の検査は **1024px までしか見ません**。広い画面でしか出ない崩れは映らない
- カタログの成功ボタンは primary で、hover が `brightness`（背景色は変わらない）。
  **壊れていても緑のまま通ります**

7 は判定を書いてわざと壊しても最初は緑でした。背景を差し替える hover を持つ
`outline` のボタンをカタログに足して、ようやく赤くなっています。
**「検査は通っている」と「検査対象に入っている」は別のこと**——
v0.5.5 で書いた教訓に、同じ形で再会しました。

### 入口を npm に置かない

`npx create-webtemplate` と案内するには npm への publish が要ります。
**publish しません。** 代わりに tarball の URL を配ります。

```bash
npx https://nasu726.github.io/WebTemplate/create-webtemplate.tgz my-site
```

npm は URL の tarball をそのまま受け取れます（実測）。
`npx github:Nasu726/WebTemplate` は**使えません** — リポジトリ直下に `bin` が無く、
`template/` は commit していない生成物なので空のまま配られます。

### `@nasu/…` は `registries` の宣言が要る

本物の CLI を通して分かりました。利用者の `components.json` に

```jsonc
{ "registries": { "@nasu": "https://…/r/{name}.json" } }
```

が無いと `Unknown registry "@nasu"` で止まります。
**URL を直に `add` する形では、依存を辿るところで失敗します。**
再現側の検査（`verify-install.mjs`）はこの一手を知らなかったので、
**ずっと緑のままでした。** 再現と本物は役割が違うので、両方置いています。

### Windows で分かったこと

**コードを読んで書いた Windows 対応のうち、2 つは実際には間違っていました。**

| 書いてあった対応 | 実際 |
|---|---|
| `spawn("pnpm")` → `pnpm.cmd` にすれば動く | **動きません。** Node 20.12 以降、バッチを shell 無しで spawn すると EINVAL（CVE-2024-27980 の対策） |
| Windows では `child.kill()` で止める | **孫が生き残ります。** temp を掴んだままになり、判定が全部緑なのに終了コードだけ 1 になる |
| `pack.mjs` の `tar` はそのままで動く | 正しい（bsdtar 3.8.4 で確認） |

さらに、`spawnSync` は起動に失敗しても**例外を投げません**。
`r.error` を見ていなかったので、型検査もビルドも「出力が空の ✗」として
通り過ぎていました。判定が落ちたのか、プロセスが起動しなかったのかが
**一覧からは区別できません**。

### ついでに見つかった本物の欠陥 2 件

**1. タップ領域の保証が縦だけだった。** `min-h-11` は高さしか決めません。
`RSS` のような短いラベルは幅 23px にしかならず、WCAG 2.1 AA の 24px を
下回ります。**しかもフォント次第**で、Linux の CI では緑のまま通っていました。

**2. tokens.css への依存が 7 項目で宣言漏れだった。** `gap-sm` や `wt-tap` は
import 文から辿れないので、人の目でも既存の検査でも見つかりません。
`check-registry-deps.mjs` が CSS の class も突き合わせるようにしました。

## v0.9b — 実害の修正と、検査の境界の作り直し

計画は [docs/plan-v09b.md](docs/plan-v09b.md)、結果は [docs/result-v09b.md](docs/result-v09b.md)。
受けた外部レビューは [docs/review-external-v09a.md](docs/review-external-v09a.md)。

v0.9a を公開したあと、**外部 AI から公開停止（HOLD）を含むレビュー**が届きました。

- [x] `useAction` の二重実行を 2 件直す（**配布物の欠陥**）
- [x] リリース判定を利用者の経路（npm）に合わせる
- [x] 検査を並列に（280 秒 → 169 秒）
- [x] カタログとデモを公開する
- [x] 受け口を fail closed に / `.env` / Node 22.12 / 危険なコマンド / 文書の食い違い

### 最重要の判定は誤報だった。ただし、その背後の指摘は正しい

レビューの唯一の Blocker は「astro の版が npm に存在しない」でしたが、
**実在します**（install → build まで通っています）。

問題は、**こちらがそれを機械で否定できなかった**ことです。手元には
node_modules があるので、宣言が間違っていても気づけません。気づくのは
まっさらな環境の利用者が install で止まったときです。

> Green CI is not the problem.
> Green CI の意味を広く解釈しすぎることが問題です。

緑の CI が意味していたのは「部品は動く」であって、
**「利用者が始められる」ではありませんでした。**

### 二重実行（実測の数字がそのまま出た）

| 条件 | 直す前 | 直した後 |
|---|---|---|
| `onSuccess` が投げる + `retry=3` | **4 回** | 1 回 |
| 150ms 待つ guard を 5 連打 | **5 回** | 1 回 |

決済・メール・登録・削除が二重に走ります。「連打しても 1 回」はこの部品が
契約として売っているものなので、最優先で直しました。

既存の「5 回連打しても 1 回」の判定は guard の無い経路を見ており、
**この race を見ていませんでした。**

### 覚えておくこと

- **利用者が打つ道具で検査する。** 生成物の README は `npm install` と
  書いているのに、検査は pnpm で回していた。別の道具を確かめても、
  利用者の経路を確かめたことにならない
- **手元の node_modules を見ない。** 宣言が間違っていても手元では気づけない
- **速さのために読みやすさを捨てない。** 並列にしても、出力は定義順に出す
- **安全そうに見えて効いていない状態が、いちばん危ない。** CORS は認証でも
  bot 対策でもない。効かないものを効くように書かない

## v0.9c — ドキュメントと雛型（未着手）

- [ ] エディタの補完とスニペット（`registry.json` から生成）
- [ ] `apps/site` を 3 つ目の雛型に（ブログ付きサイト）
- [ ] 残りの P2/P3（外部レビュー参照）
- [ ] Renovate で依存更新 → CI が緑ならタグを進める

## v0.10 — GUI ナビゲータ（未着手）

**localhost で開く web アプリ**にします（FastAPI の docs のような形）。
ウィンドウを自前で作りません。別パッケージにして本体を汚さない形にします。

## 判断待ち

### ビジュアル編集
**方針: 当面やらない。** 「コードを全く書けない人」は対象外と決めたため。
やるとしても、レイアウト部品が揃った後に「限られた選択肢から選ぶ UI」として
載せる形にします。先に作ると無限の CSS 値を吐いてきれいなコードになりません。

競合調査の結果、Builder.io / Plasmic / Makeswift の 3 つとも
ページ内容を独自のクラウド形式で保存しており、セルフホストの選択肢がありません。
「ロックインしない」を満たせているものは無い、というのが唯一の勝ち筋です。

### 認証・課金の配線
有償ボイラープレートが $200〜600 取っている領域。アダプタ方式にすれば
バックエンドに依存せず作れます。ただし静的サイト中心の方針と直交するので後回し。

### 生成後の更新パス
JS 圏には Python の `copier` / `cruft` に相当するものがありません。
独立したツールとして切り出す価値はありますが、この規模で手を出すものではありません。
